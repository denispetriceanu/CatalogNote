<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.catalognote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChatActivity"></activity>
        <activity android:name=".teacher.AddNotes" />
        <activity android:name=".teacher.classWork.ShowStudents" />
        <activity android:name=".students.ShowNotes" />
        <activity android:name=".students.ListClassForStudents" />
        <activity
            android:name=".teacher.classWork.EditInfoClass"
            android:theme="@style/SCBSwitch" />
        <activity
            android:name=".log.FirstActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".teacher.ListOfClassTeacher"
            android:parentActivityName=".MainActivity"
            android:theme="@style/SCBSwitch" />
        <activity
            android:name=".log.Login"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".log.Register"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>